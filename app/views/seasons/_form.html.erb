<%= form_with(model: [season.produce, season]) do |form| %>
  <% if season.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(season.errors.count, "error") %> prohibited this season from being saved:</h2>

      <ul>
        <% season.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-3">
      <div class="mb-3">
        <%= form.label :place, class: "form-label", style: "display: block" %>
        <%= form.text_field :place, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :start_time, class: "form-label", style: "display: block" %>
        <%= form.number_field :start_time, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :end_time, class: "form-label", style: "display: block" %>
        <%= form.number_field :end_time, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.submit class: "btn btn-primary" %>
      </div>
    </div>
  </div>
<% end %>

<%= button_to(
      vouch.value ? t(:downvote) : t(:upvote),
      vouch_path(vouch),
      {
        method: :patch,
        params: {
           vouch: {
             value: !vouch.value
           }
         },
         class: "btn btn-#{vouch.value ? 'danger' : 'success' }"
      }
    ) %>

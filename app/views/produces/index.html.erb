<% if @in_season_produces.any? %>
  <section id="in-season-now" class="mb-5">
    <h1>In season now</h1>
    <%= render 'produces', produces: @in_season_produces %>
  </section>
<% end %>

<section id="search" class="mb-5">
  <h1>Other produce</h1>
  <div class="row mb-3">
    <%= render 'search_form' %>
  </div>
  <div class="row">
    <% if @other_produces.any? %>
      <div id="produces">
        <%= render 'produces', produces: @other_produces %>
      </div>
    <% else %>
      <p>We don't have that produce. Would you like to create it?</p>
    <% end %>
  </div>
</section>

<% if @unknow_season_produce.any? %>
  <section id="season-unknown" class="row mb-5">
    <h1>Season unknown</h1>
    <%= render 'produces', produces: @unknow_season_produce %>
    <p>If you know the season for some of these, please add them.</p>
  </section>
<% end %>

<%= link_to "Add produce", new_produce_path, class: 'btn btn-primary' %>
